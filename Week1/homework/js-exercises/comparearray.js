var a = ['one', 'two', 'three', 'four'];
var b = ['hello', 123,  true, false, 7.75, 'bye',20];

if (a.length == b.length) {
  console.log('they are the same') 
} else {
  console.log('two different size')
}